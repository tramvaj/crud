# CRUD

A basic Next.js app for managing items.

## Setup

1. Install dependencies: `npm install`
2. Run dev server: `npm run dev`
3. Open http://localhost:3000

## Deploy

Push to GitHub and deploy on Vercel.

Note: File storage won't persist on Vercel. Use a database for production.